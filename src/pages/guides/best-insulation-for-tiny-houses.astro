---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProductCard from '../../components/ProductCard.astro';
import AffiliateDisclosure from '../../components/AffiliateDisclosure.astro';
import JsonLd from '../../components/JsonLd.astro';
import products from '../../data/products.json';
const topPicks = [
  products.find(p => p.slug === 'spray-foam-insulation-kit'),
  products.find(p => p.slug === 'tiny-house-toolkit'),
  products.find(p => p.slug === 'tiny-house-door-lock')
].filter(Boolean);
const pageTitle = 'Best Insulation for Tiny Houses';
const pageDesc = 'Our top picks for insulation and energy efficiency for tiny homes.';
const canonical = 'https://thetinyhousenation.com/guides/best-insulation-for-tiny-houses/';
const breadcrumb = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  'itemListElement': [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://thetinyhousenation.com/' },
    { '@type': 'ListItem', position: 2, name: 'Guides', item: 'https://thetinyhousenation.com/guides/' },
    { '@type': 'ListItem', position: 3, name: pageTitle, item: canonical }
  ]
};
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebPage',
  headline: pageTitle,
  description: pageDesc,
  datePublished: '2025-01-10',
  dateModified: '2025-01-10',
  mainEntityOfPage: canonical,
  author: { '@type': 'Organization', name: 'The Tiny House Nation' }
};
---
<BaseLayout title={pageTitle} description={pageDesc} canonical={canonical}>
  <Fragment slot="head">
    <JsonLd data={jsonLd} />
    <JsonLd data={breadcrumb} />
  </Fragment>
  <h1>{pageTitle}</h1>
  <p class="meta">Last updated: 2025-01-10</p>
  <p>Looking for the best insulation for your tiny house? Here are our top picks for energy efficiency, plus tips for choosing the right insulation.</p>
  <section>
    <h2>Top Picks</h2>
    <AffiliateDisclosure />
    <div style="display:flex;flex-wrap:wrap;gap:1.5rem;">
      {topPicks.map(product => <ProductCard product={product} key={product.slug} />)}
    </div>
  </section>
  <section>
    <h2>Comparison Table</h2>
    <table>
      <thead>
        <tr><th>Product</th><th>Price</th><th>Pros</th><th>Cons</th><th>Best for</th></tr>
      </thead>
      <tbody>
        {topPicks.map(product => (
          <tr>
            <td>{product.name}</td>
            <td>{product.priceNote}</td>
            <td>{product.pros.join(', ')}</td>
            <td>{product.cons.join(', ')}</td>
            <td>Energy efficiency</td>
          </tr>
        ))}
      </tbody>
    </table>
  </section>
  <section>
    <h2>How to Choose</h2>
    <p>Consider R-value, installation method, and climate. Good insulation saves energy and increases comfort.</p>
  </section>
  <section>
    <h2>FAQs</h2>
    <details><summary>What is R-value?</summary><div>R-value measures insulation’s resistance to heat flow. Higher is better.</div></details>
    <details><summary>Can I install insulation myself?</summary><div>Many products are DIY-friendly, but some require professional help.</div></details>
    <details><summary>Is spray foam safe?</summary><div>When installed correctly, spray foam is safe and effective.</div></details>
  </section>
</BaseLayout>
